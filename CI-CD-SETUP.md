# Настройка CI/CD для vueapp.itcenter.pro

## Быстрая настройка

### 1. Добавьте Secrets в GitHub

Перейдите в ваш репозиторий на GitHub:
1. Откройте **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **New repository secret**
3. Добавьте следующие secrets:

#### Обязательные secrets:

| Имя | Значение |
|-----|----------|
| `SERVER_HOST` | `vueapp.itcenter.pro` |
| `SERVER_USER` | `vueapp_itcen_usr` |
| `SERVER_PASSWORD` | `,u{,%qpfHr|qH`1(` |

#### Опциональные secrets:

| Имя | Значение | Описание |
|-----|----------|----------|
| `SERVER_PORT` | `22` | Порт SSH (по умолчанию 22) |
| `SERVER_PATH` | `~/public_html` | Путь на сервере для деплоя (обычно `public_html` в домашней директории) |

**Примечание:** Если путь на сервере отличается, укажите полный путь, например: `/home/vueapp_itcen_usr/public_html`

### 2. Автоматический деплой

После настройки secrets деплой будет происходить автоматически при каждом push в ветку `main` или `master`.

### 3. Ручной запуск деплоя

Если нужно запустить деплой вручную:

1. Перейдите в **Actions** в репозитории
2. Выберите workflow **Deploy to Server**
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

### 4. Проверка работы

После деплоя проверьте:
- Главная страница: https://vueapp.itcenter.pro/
- Страница установки: https://vueapp.itcenter.pro/install.php

## Что делает CI/CD

1. ✅ Проверяет код из репозитория
2. ✅ Устанавливает зависимости (`npm ci`)
3. ✅ Собирает проект (`npm run build`)
4. ✅ Копирует файлы из `dist/` на сервер через SSH
5. ✅ Устанавливает права доступа к файлам

## Устранение проблем

### Ошибка подключения по SSH

- Проверьте правильность `SERVER_HOST`, `SERVER_USER` и `SERVER_PASSWORD`
- Убедитесь, что SSH доступен на сервере (порт 22)
- Проверьте, что пользователь имеет права на запись в `SERVER_PATH`

### Файлы не появляются на сайте

- Проверьте правильность пути `SERVER_PATH`
- Убедитесь, что файлы копируются в правильную директорию
- Проверьте права доступа к файлам на сервере

### Ошибки сборки

- Проверьте логи в GitHub Actions
- Убедитесь, что все зависимости установлены
- Проверьте версию Node.js (должна быть 20.x или выше)

## Безопасность

⚠️ **Рекомендации:**
1. Используйте SSH ключи вместо пароля (см. `.github/SECRETS.md`)
2. Ограничьте доступ к репозиторию
3. Регулярно обновляйте пароли
